export enum Roles {
  SUPER_ADMIN = 1,
  ADMIN = 2,
  SHOP_OWNER = 3,
  CHECKER = 4,
  MANAGER = 5,
}

export enum OrderStatus {
  PENDING = 1,
  COMPLETED = 2,
  CANCELLED = 3,
}

export enum OrderType {
  LOADING = 1,
  PAHABOL = 2,
  ADDITIONAL = 3,
}

export enum TransactionStatus {
  PENDING = 1,
  COMPLETED = 2,
  CANCELLED = 3,
}
